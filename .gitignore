# Source media — stored in R2, NOT in git
pipeline/data/raw/

# Pipeline outputs that get uploaded to R2 (regenerable)
pipeline/data/processed/video/

# Node
web/node_modules/
web/.next/

# Python
__pycache__/
*.pyc
.venv/

# Env / secrets
.env
.env.local

# OS
.DS_Store

# Per-clip telemetry tracks (regenerable, goes to R2)
pipeline/data/processed/tracks/
web/public/tracks/
